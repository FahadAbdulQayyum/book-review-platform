import Form from './form'
const ReviewForm = () => {
    return (
        <div>
            <Form isFor={'Review Form'} additionalInputs={['review']} />
        </div>
    )
}

export default ReviewForm
